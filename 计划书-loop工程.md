# 个人工具箱项目 · Loop 工程计划书

> 版本：v1.0  
> 目标：基于 `my-tools-web` 逐步交付可用、可迭代、可维护的在线工具箱  
> 日期：2026-07-29

## 1. 项目目标

- 交付一个无登录、无广告、纯前端的开发者工具箱（MVP 可直接日常使用）
- 所有处理在浏览器本地完成，不上传用户内容
- 通过“小而快”的循环（Loop）持续交付，优先解决高频 P0 需求
- 保持结构可扩展：新增工具只改 `src/tools/index.ts`（注册）与工具目录（页面 + 逻辑）

## 2. 范围说明（In Scope）

- 已有功能：JSON、Base64、URL、时间戳、进制转换、文本处理、密码生成、Hash、JWT、Markdown 预览、二维码、颜色转换、正则、单位转换、文本差异
- 目标补齐：缺失路由/页面闭环项（如图片压缩）
- 平台支撑：主页搜索、分类、最近使用、暗色模式、404、隐私页、Toast

## 3. 当前状态（2026-07-29）

- 已完成骨架：工具注册、路由自动映射、主页、布局、主题、最近访问、多个核心工具
- 已发现缺口：
  - 图片压缩工具仅有 `utils.ts`，缺 `index.ts + index.vue`（未在首页和路由中露出）
  - 若干安全/体验风险（文档化建议）：JWT 解码 base64url 兼容、Markdown `v-html` 安全、二维码工具可考虑下载名与大小提示
  - 部分文案与 SEO、GitHub 地址、路由方式（静态部署兼容）可优化

## 4. Loop 交付模型

每个 Loop = 1~2 周，包含：
- Loop 输入：需求与缺陷清单
- Loop 输出：可部署版本与验收结果
- Loop 评审：是否满足验收标准、是否进入下一 Loop

### Loop 0（基线校准，1 天）
- 目的：统一规则与验收口径
- 任务：
  - 锁定文档版本（需求书、功能书、技术栈说明书）
  - 固定工具分类与导航规范
  - 明确“新增工具”清单及优先级
- 验收：
  - 形成本计划书 V1.0
  - 所有团队成员（你）确认优先级与里程碑

### Loop 1（功能闭环，3-5 天）
- 目的：补齐 MVP 闭环能力
- 任务：
  1. 完成“图片压缩”工具页面与注册
     - 新增 `src/tools/image-compressor/index.ts`
     - 新增 `src/tools/image-compressor/index.vue`
     - 在 `src/tools/index.ts` 中注册
  2. 检查最近使用与主页分类展示稳定性
  3. 简化所有工具页交互一致性（返回/清空/复制反馈）
- 验收：
  - 首页可点中每个已开发工具并正常打开
  - 图片压缩从“可上传/压缩/预览/下载”流程完整

### Loop 2（体验修复，3-5 天）
- 目的：提高日用体验与安全性
- 任务：
  1. 对所有输入/输出文案做一致化（按钮文案、错误提示）
  2. 增加关键输入校验与失败提示（空输入、非法参数）
  3. JWT 解析兼容 base64url 与长度异常提示
  4. Markdown 预览增加输出净化策略（如 DOMPurify 或白名单渲染策略）
  5. 404、隐私页与主页 Footer 文案补齐（GitHub 地址替换为真实仓库）
- 验收：
  - 核心错误输入下无空白失败/控制台异常
  - 安全审查点通过（至少处理 markdown 渲染风险）

### Loop 3（上线准备，2-3 天）
- 目的：部署可访问、可被索引
- 任务：
  1. Vercel 部署脚本与环境配置确认（`vite build` 可复现）
  - 2. GitHub Pages 兼容性核对（history fallback / 路由处理）
  3. 首屏性能与基础静态资源检查（首屏 < 3s）
  4. 建立上线检查清单（README/隐私声明/版本记录）
- 验收：
  - 部署后可稳定访问首页与工具页
  - 路由 404、返回首页逻辑正确

### Loop 4（增强能力，1-2 周）
- 目的：补齐 P1 功能并提升可维护性
- 任务：
  - 视觉工具（Markdown/颜色转换/二维码）体验打磨
  - 搜索体验优化（高频关键字命中）
  - 工具元数据字段增强（`keywords/tags/category`）
  - 日志与回归脚本（人工可复测清单）
- 验收：
  - P1 工具按功能书可复测通过
  - 新增一个工具不改动路由文件的可交付流程

## 5. 每轮任务模板（执行口径）

每轮请按以下 6 步执行：
1. 任务分解（工具级）  
2. 实现（代码变更）  
3. 交互与错误用例自测  
4. 构建验证（`npm run build`）  
5. 计划书更新（状态勾选）  
6. 提交版本说明（本轮变更摘要）

## 6. 验收标准（全局）

- P0 核心工具：7 个全部可用，错误提示明确
- 首页：搜索、分类、最近使用、路由跳转正常
- 安全：无外发接口，敏感输入仅本地计算
- 交付：每次 Loop 都有可部署产物

## 7. 风险与缓解

- 风险：新工具未关闭安全边界（Markdown/外部输入）  
  - 缓解：逐个工具输出白名单与输入校验
- 风险：部署路由 404（SPA fallback）  
  - 缓解：在部署环境添加 fallback 配置
- 风险：功能扩展导致维护负担增加  
  - 缓解：严格工具目录规范（`index.ts`、`index.vue`、`utils.ts`）

## 8. 变更记录

- v1.0（2026-07-29）：创建 Loop 工程计划书（Loop 0~4）
- v1.0+（2026-07-29）：Loop 1 闭环交付完成（图片压缩工具注册、页面、交互、构建通过）
- v1.1（2026-07-29）：Loop 2（体验修复）已开始：JWT base64url 兼容增强、Markdown 输出净化、404/隐私页/首页文案与 GitHub 链接补齐
- v1.2 (Loop3 start) 2026-07-29: Added GitHub Pages SPA fallback via public/404.html + root query restore in main.ts; added Vercel SPA rewrites via vercel.json; README deployment notes updated.
- v1.3 (2026-07-29): Added GitHub Pages deployment robustness files (`public/404.html`, `public/.nojekyll`) and documented manual deployment steps in README.
- v1.4 (2026-07-29): Added tool route aliases support using Tool.redirectFrom, with Image Compressor alias `/tools/compress-image`.
- v1.5 (2026-07-29): Loop4 搜索体验增强：Tool metadata 增加 `tags` 字段，首页搜索支持分词匹配与评分排序，并支持 `cat:` 与 `tag:` 前缀筛选。

## 9. 长任务（Loop 5）

目标（2026-07-30~2026-08-13）：把“能用”提升为“稳定可维护、可复用、可上线复盘”的平台状态。

### 任务范围（完整闭环）

- 平台级体验优化（Search/Recent/导航/反馈）
- 数据一致性与安全边界再确认（输入清洗、降级提示、异常展示）
- 文案与编码治理（全站中文显示一致性、关键文案统一）
- 发布与回归规范（构建、部署、冒烟、可复测清单）
- 可观察性增强（版本记录、变更回滚策略、快速定位方式）

### 长任务拆解（7子项，按优先级）

1. 搜索/发现体验增强（P0）
   - 给 `HomePage` 增加“快捷命令”入口文案提示：`cat:` 分类、`tag:` 标签、`/` 空格组合。
   - 搜索结果展示命中原因（高亮命中字段/匹配权重说明）提升可解释性。
   - 搜索空状态与错误输入引导文案统一（含“清空/重试”）。
   - 验收：查询响应 < 150ms（本地静态数据量 20+工具），相关度排序稳定。

2. 工具元信息与可发现性统一（P1）
   - 完善 `Tool` metadata：`keywords`、`tags`、`category`、`path/redirectFrom` 的命名一致性清单。
   - 给每个工具补上“使用场景一句话”（description）。
   - 新增/新增别名路由时统一更新 `redirectFrom` 与文档入口。
   - 验收：所有工具 metadata 与目录文件一一对应，至少 16 个场景关键字覆盖。

3. 工具页一致性改造（P1）
   - 统一按钮文案（中文）与成功/失败反馈组件（toast/空状态）。
   - 关键输入项加最小值、最大值、非法值提示（例如数值工具、压缩器）。
   - 上传/下载类工具增加格式说明（输入/输出容量、支持列表）。
   - 验收：主要工具在异常输入下 0 崩溃，反馈可读、可重试。

4. 编码与文案治理（P1）
   - 逐一修复计划书/主页/隐私页中的乱码文本片段。
   - 建立“文件级编码检查清单”（至少：
     `src/pages/*.vue`、`src/tools/*/index.ts`、`README.md`、`计划书-loop工程.md`）。
   - 建立新工具接入文案模板（中英可读，字段齐全）。
   - 验收：`npm run build` 与预览文本中无明显乱码乱码块，中文文案一致中文展示。

5. 质量与安全回归（P1）
   - 复跑关键工具回归用例：JSON/URL/Base64/JWT/Markdown/Hash/Qrcode/Color/Regex/Text.
   - Markdown/自定义内容渲染路径保留净化策略并补充至少 5 条回归场景。
   - JWT/解码工具加入异常输入测试（长度不足、非法字符、签名缺失）。
   - 验收：回归用例全部可复现，异常用例有明确错误提示。

6. 部署与入口鲁棒性（P0）
   - 核验 Vercel/GitHub Pages 的路由 fallback 一致性（含深链访问）。
   - 更新 README 部署检查清单：构建命令、文件上传清单、回滚步骤。
   - 在首页和NotFound补充“返回首页/返回上一步”入口一致体验。
   - 验收：任意 deep link 均可 404 后正确回退，首页可完整访问。

7. 验收与交接（P2）
   - 完成 Loop5 交付说明：本次变更清单、已知风险、下个 Loop 输入项。
   - 更新计划书变更记录 + 版本号（v1.6）。
   - 形成“上线前 10 分钟 Checklist + 上线后 24h Checklist”。
   - 验收：计划书/README 与真实功能一致，无未关闭的阻塞任务。

### 交付标准（硬性）

- 可交付：`npm run build` 必过，功能层面无回归崩溃。
- 可复现：长任务包含独立验收清单，按条目打钩。
- 可维护：新增字段和文案均有注释/示例；工具接入不改路由主文件可完成。
- 可回溯：计划书记录 v1.6 + PR/提交记录可关联到每个子项。

### 目标里程碑

- 第 3 天：完成搜索提示与文案清理模板落地；
- 第 6 天：完成工具元数据统一 + 回归脚本雏形；
- 第 10 天：完成安全回归 + 部署回归；
- 第 14 天：完成验收闭环并更新 v1.6。

### Loop5 变更记录

- v1.6（预计 2026-07-30）：新增长任务版本计划（Loop 5），覆盖平台体验、元数据统一、编码治理、回归与发布闭环。

