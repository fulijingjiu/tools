# 回归检查清单

> 适用范围：搜索、工具元数据、主题顺序、路由兼容与生产构建  
> 最近自动执行：2026-08-21  
> 自动检查结果：通过

## 自动检查命令

```bash
npm run check:tools
npm run qa:loop5:search
npm run qa:loop5:ui
npm run qa:loop5:theme-order
npm run qa:loop5:compat
npm run qa:loop5
```

## 搜索功能

- [x] 空查询显示首页默认内容，而不是进入搜索结果模式。
- [x] 工具名称精确匹配可以返回预期结果。
- [x] 搜索不区分英文字母大小写。
- [x] 多关键字可以组合匹配。
- [x] 逗号和空格可以用于分隔关键字。
- [x] `cat:developer` 只返回开发分类工具。
- [x] `cat:dev` 支持分类部分匹配。
- [x] `cat:*` 返回所有具有分类的工具。
- [x] `tag:security` 返回安全相关工具。
- [x] `tag:*` 返回所有具有标签的工具。
- [x] 未知分类和标签显示无结果状态。
- [x] 裸 `cat:`、`tag:` 前缀显示使用提示。
- [x] `/`、`+`、`*` 等特殊符号不会导致异常。
- [x] 快捷提示可以写入搜索框并刷新结果。
- [x] 清空按钮可以退出搜索状态。
- [x] 结果卡片可以显示命中原因。
- [x] 切换主题不会改变搜索结果顺序。

## 工具元数据

- [x] 每个工具包含 ID、名称、路径、说明、分类、关键字、组件和图标。
- [x] 每个工具的关键字列表非空。
- [x] 每个工具的标签列表非空。
- [x] 不存在重复工具 ID。
- [x] 不存在重复主路径。
- [x] 单个工具内不存在重复关键字或标签。
- [x] 工具目录与注册表数量一致。

## 路由与构建

- [x] 路由由工具注册表生成。
- [x] 工具别名可以生成重定向路由。
- [x] SPA 回退恢复逻辑存在。
- [x] `public/404.html` 和 `public/.nojekyll` 存在。
- [x] Vercel SPA 重写配置存在。
- [x] `npm run build` 通过。
- [x] 所有跟踪的文本文件是有效 UTF-8。
- [ ] GitHub Pages 项目站点实际访问通过。
- [ ] 任意工具深链接在线刷新通过。

## 性能验收

- [ ] 使用至少 20 个工具数据执行搜索基准。
- [ ] 搜索响应 P95 小于 150ms。
- [ ] 记录测试设备、Node.js 版本、样本数量和执行结果。

## 人工界面验收

- [ ] 桌面端检查首页、搜索、分类和工具页。
- [ ] 移动端检查布局、按钮和输入框。
- [ ] 检查深色与浅色主题。
- [ ] 检查 16 个工具的基本进入和返回流程。
- [ ] 检查图片上传、压缩、预览和下载。
- [ ] 保存截图或浏览器自动化报告。

## 正式发布门槛

- [ ] 自动检查全部通过。
- [ ] 单元测试和浏览器测试全部通过。
- [ ] GitHub Pages 或 Vercel 线上地址可访问。
- [ ] README 中的部署说明与实际方式一致。
- [ ] 版本号、变更日志、Tag 和 Release 一致。
- [ ] 工作区干净，主分支与远程同步。
